package dev.tigr.mesh.api.entity.living.mob.hostile.raider;

import dev.tigr.mesh.api.entity.living.mob.hostile.EntityHostile;

/**
 * EntityWitch would come under raider for later versions, but because of 1.12 we must classify it as just an EntityHostile
 */
public interface EntityWitch<T> extends EntityHostile<T> {
}
